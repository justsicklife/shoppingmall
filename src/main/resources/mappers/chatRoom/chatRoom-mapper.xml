<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
        
<mapper namespace="s_chatRoom">
	<!-- type 리턴한 결과를 매핑할 오브젝트 타입의 패키지 경로를 포함한 전체 클래스명이나 별칭 -->
	<resultMap type="chatRoom" id="chatRoomResultMap">
		<result column="cr_id" property="chatRoomId"/>
		<result column="m_id" property="memberId"/>
		<result column="cr_title" property="chatRoomTitle"/>
	</resultMap>
	
	<select id="findById" parameterType="int" resultMap="chatRoomResultMap">
		select * from s_chat_room where m_id = #{id}
	</select>
	
	<select id="chatRoomFindAll" parameterType="int" resultMap="chatRoomResultMap">
		select * from s_chat_room
	</select>
	
	<insert id="chatRoomInsert" parameterType="chatRoom">
		<selectKey resultType="int" keyProperty="chatRoomId" order="AFTER">
			SELECT s_chat_room_seq.CURRVAL as s_chat_room FROM DUAL
		</selectKey>
		insert into s_chat_room(cr_id,m_id) VALUES(s_chat_room_seq.nextval,#{memberId})
	</insert>
	
</mapper>